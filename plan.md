Мини-проект: API → обработка → CSV/SQLite

Используй:

requests — забрать JSON из API;
json — понять работу с JSON руками;
pathlib — файлы/папки;
datetime — даты;
logging — нормальные логи вместо print;
pandas — преобразования;
sqlite3 — сохранить результат в БД.

Структура примерно:

mini_etl/
├── main.py
├── data/
│   ├── raw/
│   └── processed/
└── logs/

Само задание:

Забрать данные из какого-нибудь публичного API.
Проверить HTTP-код и обработать ошибку через try/except.
Сохранить исходный JSON как есть в data/raw/.
Превратить данные в DataFrame.
Почистить:
переименовать столбцы;
убрать ненужные;
обработать None;
привести типы.
Сделать хотя бы одну агрегацию через groupby.
Сохранить результат:
в CSV;
в SQLite.
Записать через logging:
когда загрузка началась;
сколько строк пришло;
сколько осталось после обработки;
успешно ли сохранилось;
ошибку, если что-то упало.

Не делай всё в одном main.py на 300 строк. После первой рабочей версии разнеси:

def extract():
    ...

def transform(data):
    ...

def load(df):
    ...

def main():
    ...

Вот это сейчас для тебя будет очень полезно. Ты одновременно потрогаешь функции, exceptions, requests, JSON, pandas, файлы, БД и ETL, а не будешь писать очередной калькулятор возраста, который человечество переживёт и без нас



До делать:
extract():
    raise_for_status();
    нормальное логирование текста ошибки;
    исправить len(r.json()) на количество products;
    пагинацию, чтобы забрать все товары, а не первую пачку.
read_json():
    обработать отсутствие файла;
    обработать битый JSON.
transform():
    проверка дубликатов по id;
    проверка обязательных полей;
    убрать лишний df.map(...), если списков/словарей уже нет.
load():
    соединение с SQLite через with;
    уйти от тупого if_exists="replace" к более нормальной загрузке.
После загрузки самому написать SQL к своей БД:
    средняя цена по категориям;
    топ-5 товаров по рейтингу;
    количество товаров по брендам.